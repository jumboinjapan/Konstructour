# Пример конфигурации для деплоя
# Скопируйте этот файл в .env.deploy и укажите ваши данные

# Настройки сервера
SERVER_HOST=your-server.com
SERVER_USER=your-username
SERVER_PATH=/var/www/html/constructour

# Локальный путь к build файлам (опционально)
LOCAL_BUILD_PATH=./dist

# Дополнительные настройки rsync (опционально)
RSYNC_OPTIONS="--exclude='.DS_Store' --exclude='Thumbs.db'"

# Команды для выполнения после деплоя (опционально)
POST_DEPLOY_COMMANDS="systemctl reload nginx"
